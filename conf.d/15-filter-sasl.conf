filter {

  if "sasl" in [tags] {
    grok {
      patterns_dir => ["/etc/logstash/patterns/"]
      match => [ "message", "%{SALSDRESULT}" ]
      add_tag => [ "result" ]
      remove_tag => [ "unknown" ]
    }
  }

}